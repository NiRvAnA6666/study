  LLM
  → 명령을 제안하는 두뇌

  Tool
  → 명령을 실제로 실행하는 것.

  Approval
  → 그 행동을 시도해도 되는지 판단

  Sandbox
  → 실제 행동이 미칠 수 있는 범위를 제한

  Sandbox escape
  → 그 제한 범위 밖에 영향을 주는 것

# AI에 관하여
## AI agent에서 샌드박스란
일반적인 LLM은 텍스트만 생성하지만, codex,claude,gemini_cli같은 ai_agent는 실제 도구를 사용할 수 있다.
사용자 입력,저장소 내용
-> LLM의 판단
-> Tool call생성
-> 파일 rwx, 네트워크 접근
등등

AI가 생성한 명령이사용자 컴퓨터 전체나 다른 서버, 사용자에게 영향을 주면 위험하기 때문에, 실제 행동 범위를 제한하는 장치가 필요 --> 이것이 `샌드박스(sandbox)`이다.

예를 들어 Codex에게 /home/guts/project 위 경로에서만 권한을 주고 이 공간에서 프로젝트를 실행했다고 가정하면,
work-write 샌드박스는 대부분 다음과 같이 동작한다.
/home/guts/project 내부 쓰기 -> 허용
/home/guts/project 외부 쓰기 -> 차단
네트워크 접근                 -> 정책 별 상이

샌드박스는 AI의 생각이나 답변을 제한하는 것이 아닌 AI가 요청한 명령이 실제 운영체제에서 할 수 있는 행동을 제한하는 것이다.

## 샌드박스가 필요한 이유
AI agent는 실행할 명령이 미리 정해진 프로그램이 아닌 현재 상황과 읽은 내용을 바탕으로 때마다 다른 판단을 해 명령을 실행한다.
이 과정에서 모델은 신뢰에 영향을 받을 수 있다.
- 악성 Git 저장소
- Readme와 코드 주석
- 웹 페이지
- Issue와 Pull Request
- MCP Server 응답
- - - MCP는 Anthropic에서 발표한 프로토콜로 LLM 모델이 외부 애플리케이션과 연동할 수 있도록 해주는 스펙, 예를 들어 클로드 모델이 학습된 결과로 답변만 할 수 있는데 비해서 MCP를 이용하여 외부 애플리케이션과 연동되게 하여 구글 검색 결과를 통해서 최신 정보를 가지고 답변을 하거나, Spotify를 이용해서 음악을 플레이, IDA연결을 통하여 정적 분석을 할 수 있다. (IDA정적 분석 시 MCP보단 Wine을 사용하는 것이 토큰이 더 적게 들 수 있음)
- 명령 실행 결과
- 간접 프롬포트 인젝션

모델이 잘못된 판단으로 위험한 명령을 생성하더라도 샌드박스가 제대로 작동하면 실제 피해 범위를 줄일 수 있다.
만약 모델이 판단을 실패 한다면
- 위험한 명령 생성
- 샌드박스가 파일, 네트워크 접근 차단
- 피해 제한

## Approval과 sandbox
Approval
-> 이 명령을 실행해도 되는가?
Sandbox
-> 실행된 명령이 어디까지 영향을 줄 수 있는가?
이러한 차이가 존재한다.
즉, approval은 행동을 시작할지 말지 판단하고, sandbox는 실행된 행동의 권한 범위를 제한한다.

## 추가 샌드박스 이스케이프
### 샌드박스 이스케이프란?
샌드박스 이스케이프는 샌드박스 안에 있어야 할 프로세스가 허용 범위 밖에 보안 영향을 주는 현상
예를 들면
- workspace 밖 파일 읽기, 쓰기
- 차단된 네트워크 접근
- 다른 프로세스 접근
- 사용자 credential 접근
- Contatiner나 VM 밖으로 영향 확장

모든 샌드박스 이스케이프가 루트 권한이나 완전한 호스트 탈취를 의미하진 않는다.
무슨 말이냐면 
외부 파일을 읽었을 때
=! 외부 파일 쓰기
=! 호스트 명령 실행
=! 루트 권한 획득
은 꼭 아니라는거다.

따라서 실제로 획득한 기본 자료형과 최종 영향을 구분해야 한다.

### 샌드박스 이스케이프의 두 가지 형태
#### OS 샌드박스 자체의 취약점
정상적인 정책이 설정됐지만 커널, 컨테이너 , vm 또는 샌드박스 구현의 취약점으로 경계를 탈출하는 경우다.

올바른 정책에는
-> 샌드박스 구현 취약점 이용
-> 경계 밖 접근
이 있다.
#### 애플리케이션의 정책 설정 오류
OS 샌드박스는 정상 작동하지만 상위 애플리케이션이 허용 정책을 잘못 전달하는 경우다.
애플리케이션이 잘못된 경로를 허용
-> OS 샌드박스는 전달받은 정책을 정상 적용
-> 결과적으로 경계 밖 접근 허용
이 일어나게된다.
이러한 취약점을 해외에선
- Logical sandbox escape
- Sandbox boundary bypass
- Policy bypass
- Workspace boundary bypass
이렇게 표현한다.

위 취약점에 대해서 cve 하나를 소개하자면 `CVE-2025-59532` 이것이다.
Codex CLI가 모델이 선택한 명령 실행 위치와 사용자가 결정해야 하는 샌드박스 쓰기 권한 기준을 같은 cwd(실행되는 현재 디렉터리)로 처리하면서, workspace 밖 경로가 쓰기 가능으로 권한이 승격된다.(기존 읽기만)
--8.13.(목).
 
